# ============================================================
# SamaVoie Backend — Dépendances
# Python 3.11 requis (3.14 incompatible avec ChromaDB/pydantic.v1)
# Environnement : .venv/ (créer via : python3.11 -m venv .venv)
# Installation : .venv/Scripts/pip install -r requirements.txt
# ============================================================

# --- API & Serveur ---
fastapi[all]==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.27
httpx==0.28.1

# --- Validation & Configuration ---
pydantic[email]==2.12.5
pydantic-settings==2.14.0
python-dotenv==1.2.2

# --- RAG & IA ---
# --- PyTorch CUDA 12.4 (GTX 1650 Ti — compute 7.5) ---
# cu121 limité à torch 2.5.1 → incompatible avec transformers (exige torch>=2.6, CVE-2025-32434)
# Installation séparée obligatoire (index-url spécial) :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

langchain==1.2.15
langchain-community==0.4.1
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2          # Chunking (512 tokens, overlap 50)
chromadb==1.5.8
sentence-transformers==5.4.1             # BGE-M3 local (symétrie indexation/retrieval)
# ATTENTION : google-generativeai est déprécié — migrer vers google-genai (Phase 3)
google-generativeai==0.8.6               # Gemini 1.5 Pro — extraction PDF

# --- Base de données & Migrations ---
sqlalchemy==2.0.49
asyncpg==0.31.0                          # Driver async PostgreSQL (runtime)
psycopg2-binary==2.9.12                  # Driver sync (Alembic offline mode)
alembic==1.18.4                          # Migrations de schéma

# --- Auth & Sécurité ---
python-jose[cryptography]==3.5.0         # JWT tokens
passlib[bcrypt]==1.7.4                   # Hachage des mots de passe
bcrypt==4.0.1                            # passlib 1.7.4 incompatible avec bcrypt>=4.1
